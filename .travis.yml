language: node_js
node_js:
  - lts/*
install:
  - npm install
script:
  - npm build
cache: npm
deploy:
  provider: pages
  skip-cleanup: true
  keep-history: true
  local-dir: dist
  github-token: $GITHUB_TOKEN
  on:
    branch: master
